# Dependencies bundled into the pre_result_alert Lambda zip.
# The backend/ package is included separately by the deploy script.
#
# aiohttp: WhatsApp Cloud API (raw HTTP sends bypass AlertPayload formatters)
# python-telegram-bot: Telegram direct sends
# psycopg[binary]: sync DB access (latest signal lookup per symbol)
# requests: NSE results calendar scrape (same pattern as nse_poller)
aiohttp>=3.10
python-telegram-bot>=21.0
boto3>=1.35
psycopg[binary]>=3.2
pydantic>=2.9
python-dotenv>=1.0
requests>=2.32
